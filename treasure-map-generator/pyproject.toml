[project]
name = "treasure-map-generator"
version = "1.0.0"
description = "ロジックたからさがし ─ たからのちず PDF ジェネレータ"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "pygments>=2.17",
    "weasyprint>=68.0",
]

[project.scripts]
generate = "generate:main"
