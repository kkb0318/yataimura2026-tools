"""
フィールドマップのデータ定義

マップを変更したい場合はこのファイルを編集してください。
座標は (行, 列) で指定します。左上が (1, 1) です。
"""

from __future__ import annotations
from dataclasses import dataclass, field


# ---------------------------------------------------------------------------
# グリッドサイズ
# ---------------------------------------------------------------------------
GRID_ROWS = 5
GRID_COLS = 10


# ---------------------------------------------------------------------------
# お宝の定義
# ---------------------------------------------------------------------------
@dataclass
class Treasure:
    """お宝の定義"""

    label: str  # マップ上のラベル (A, B, C, ...)
    row: int  # 行 (1-indexed)
    col: int  # 列 (1-indexed)
    name: str  # フルネーム
    short: str  # マス内に表示する短縮名

    # 元データにあったicon (PDF描画には使わないが参照用に保持)
    icon: str = ""


TREASURES: list[Treasure] = [
    Treasure("A", 1, 2, "伝説の剣", "剣", "⚔️"),
    Treasure("B", 1, 5, "おいしい飴", "飴", "🍬"),
    Treasure("C", 1, 7, "魔法の杖", "杖", "🪄"),
    Treasure("D", 2, 8, "金のコイン", "コイン", "🪙"),
    Treasure("E", 3, 3, "虹色の羽", "羽", "🪶"),
    Treasure("F", 3, 9, "古代の地図", "地図", "🗺️"),
    Treasure("G", 4, 1, "ドラゴンのうろこ", "竜", "🐉"),
]


# ---------------------------------------------------------------------------
# 数字マス  (行, 列, 表示する数字)
# ---------------------------------------------------------------------------
NUMBER_CELLS: list[tuple[int, int, int]] = [
    (1, 3, 2),
    (1, 8, 4),
    (2, 2, 3),
    (2, 5, 2),
    (3, 1, 4),
    (3, 4, 1),
    (3, 7, 2),
    (3, 10, 2),
    (4, 3, 1),
    (4, 5, 2),
    (4, 8, 1),
    (5, 2, 3),
    (5, 9, 3),
]


# ---------------------------------------------------------------------------
# 障害物  (行, 列)
# ---------------------------------------------------------------------------
OBSTACLES: list[tuple[int, int]] = [
    (5, 5),
    (5, 6),
]


# ---------------------------------------------------------------------------
# スタート位置
# ---------------------------------------------------------------------------
@dataclass
class StartPosition:
    """スタート位置の定義"""

    row: int
    col: int
    lines: list[str] = field(default_factory=list)  # マス内に表示するテキスト行


ROBOT_START = StartPosition(5, 4, ["人間ロボット", "ゲーム", "スタート"])
LOGIC_START = StartPosition(5, 7, ["ロジック", "宝探し", "スタート"])


# ---------------------------------------------------------------------------
# タイトル
# ---------------------------------------------------------------------------
TITLE = "人間ロボットゲーム ＆ ロジック宝探し"
SUBTITLE = "フィールドマップ（運営用）"
FOOTER = "※ 運営スタッフ用　参加者には配布しないでください"
